name: cyberguard
description: Cybersecurity intelligence mobile application built with clean Flutter architecture.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  fl_chart: ^0.69.0
  file_picker: ^8.1.2
  flutter_local_notifications: ^17.2.3
  go_router: ^14.2.7
  provider: ^6.1.2
  workmanager: ^0.9.0+3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
